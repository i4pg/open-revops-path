# RevOps Starter Kit — Python dependencies for the code folders.
# Pin exact versions once your environment is stable.

# data wrangling (04-subsidy-waterfall, all)
pandas>=2.0
numpy>=1.24
pyarrow>=14.0        # parquet I/O for data/

# credit-risk model (05-credit-risk)
scikit-learn>=1.3

# forecasting (06-forecast) — one or both backends
statsmodels>=0.14
prophet>=1.1         # optional; drop if you only use SARIMAX

# config / secrets
python-dotenv>=1.0
pyyaml>=6.0          # read 02-metrics-dictionary/metrics.yml

# viz (waterfall charts, backtests) — optional
matplotlib>=3.7

# dev / quality — optional
# ruff>=0.4
# pytest>=8.0
